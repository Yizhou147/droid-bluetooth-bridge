# DRM 桌面原生蓝牙方案（HCI 客户端化 / droid-bluetooth-bridge）

> 状态：**方案定案，待 M0 探测**（2026-09-24 起草）
> 目标设备：Xiaomi Pad 8 Pro（piano / 25091RP04C）· HyperOS · KernelSU · 6.6.118-android15-8-ga6a16c2570b25-ab10004893-4k
> 关联：`../工作总结.md` 5.11（旧 hci_qca 路线，本文件将其作废）、`droid-drm-takeover/desk-takeover.sh`、`droid-drm-takeover/scripts/desk-stop.sh`

---

## 0. 目标与验收标准

**要什么**：进入 DRM 接管桌面后，Plasma 的蓝牙体验与正常 Linux 桌面**无差别**：

1. 任务栏蓝牙插件（bluedevil）能扫到设备、点配对、显示已连接；系统设置的蓝牙页可用（不是自造 UI）。
2. 蓝牙键盘/鼠标连上后，在 DRM 桌面的任意窗口里直接可打字、可指点（不需要任何额外切换）。
3. 回安卓（点「返回安卓」）后，安卓自己的蓝牙、已配对设备、蓝牙网络共享全部照常，**不需要重启**。
4. anland 模式（安卓完整运行、容器只画窗口）完全不受影响——那时本方案的组件一个都不运行。

**不验收（本期明确不做）**：A2DP/HFP 音频。理由：DRM 期间安卓 AudioFlinger 是停的，容器声音本来就没有出口，先把 HID 打通。

---

> **术语纪律（09-24 用户纠正）**：DRM 与 anland 是**互斥的两种显示方式** —— 进 DRM 后 anland 会话（kwin/plasmashell）已被
> desk-takeover 杀掉，**不存在"在 anland 侧操作"**；仍然在的是**容器（Linux 侧）**本身，adb 也是从容器连本机 adbd。
> 本文所说"anland 态/常态"一律指**未接管的普通显示模式**，只是环境描述，不是操作位置。

## 1. 实测事实基线（2026-09-24，全部只读取证）

| # | 事实 | 取证命令 | 实测输出/结论 |
|---|---|---|---|
| F1 | 内核蓝牙核心**是模块且已加载** | `lsmod \| grep -iE "^bluetooth\|^hci\|^bt"` | `bluetooth 716800 12 hci_uart,btsdio,rfcomm,btbcm,btqca,hidp`；`hci_uart`、`btqca`、`hidp`、`rfkill 36864 4 btpower,cfg80211,nfc,bluetooth` |
| F2 | 安卓蓝牙**正常工作但内核里没有 adapter** | `ls /sys/class/bluetooth/` | **空目录**（同一时刻安卓侧 State=ON/已连接）→ HCI 从不进内核蓝牙协议栈 |
| F3 | HCI 真通路 = glink 到 CP，且**被 vendor HAL 独占** | `ls -l /proc/*/fd \| grep -E "bt_cp_ctrl\|ttyHS0"` | 同一个 bluetooth uid 进程**同时持有** `/dev/ttyHS0`(c473:0) 与 `/dev/bt_cp_ctrl`(c478:16) |
| F4 | QTI 蓝牙 HAL 是 `class hal`，`stop` 不动它 | `getprop \| grep init.svc.*bluetooth` | `init.svc.vendor.bluetooth-aidl-qti = running`（与 display composer 同规律） |
| F5 | HAL 以标准 AIDL 暴露 HCI | `service list \| grep -i bluetooth` | `android.hardware.bluetooth.IBluetoothHci/default`；另有 `...audio.IBluetoothAudioProviderFactory`、`...finder`、`...lmp_event`、`vendor.qti...bluetooth_sar`、`xpanprovider` |
| F6 | binder 名字服务是**纯 AIDL 制** | `getprop \| grep -E "init.svc.(v?)?h?wservicemanager"` | `servicemanager=running`、`vndservicemanager=running`、`hwservicemanager=stopped` |
| F7 | 芯片电源有**正经的 rfkill 通道** | `for d in /sys/class/rfkill/rfkill*; do cat $d/type $d/name $d/soft; done` | `rfkill0: type=bluetooth name=bt_power soft=1`（`rfkill1`=wlan/phy0）→ btpower 用内核 rfkill 类做上下电 |
| F8 | `hci_uart` 的 H4 协议在内核里 | `zcat /proc/config.gz \| grep CONFIG_BT_HCIUART` | `CONFIG_BT_HCIUART=m`、`_H4=y`、`_LL=y`、`_BCM=y`、`_QCA=y`；`# ..._NOKIA/BCSP/ATH3K/3WIRE/INTEL/RTL/AG6XX/MRVL is not set` |
| F9 | 内核里**没有 vhci** | `zcat /proc/config.gz \| grep HCIVHCI` | `# CONFIG_BT_HCIVHCI is not set` |
| F10 | 固件确实删了 hci_qca 本体 | `ls /vendor/lib/modules \| grep -iE "hci\|qca"` | 只有 `btpower.ko`、`qca_cld3_peach_v2.ko`(WiFi)、`bt_fm_*`；**无 hci_qca.ko / hci_ipc.ko** |
| F11 | 容器侧 BlueZ 齐活 | `which hciattach bluetoothd rfkill`；`busctl --system list` | `/usr/bin/hciattach`、`bluetoothd 5.85`，且 `org.bluez` 已在 system bus 上（:1.51, bluetooth.service） |
| F12 | **容器里 binderfs 已经挂着，三个节点都在** | `ls -lZ /dev/{binder,hwbinder,vndbinder}` | major **506**，minor 1/2/3，组 `droidspaces-gpu`，带 SELinux label → 容器内可直接开 binder |
| F13 | 当前 SELinux **Enforcing**，KernelSU 的 su 域是 `ksu` | `getenforce`；`id -Z` | `Enforcing`；`u:r:ksu:s0`；`/sys/fs/selinux/load` 可写；`/data/adb/ksu/bin` 内**无 magiskpolicy**；KernelSU 模块目录 `/data/adb/modules`（已有 `anland-daemon`、`droidspaces` 等 15 个） |
| F17 | **pty + hci_uart(H4) 能在共享内核里凭空造出 hci0（M1 承重假设成立）** | `/data/local/tmp/m1-probe 90`（安卓侧 root） | `TIOCSETD N_HCI OK` → `第1次 HCIUARTSETPROTO(H4) rc=0` → **`/sys/class/bluetooth` 出现 `hci0`**；容器侧 `hciconfig -a` 读到 `Type: Primary  Bus: UART`、`TX commands:1`。**零新内核模块、零 insmod** |
| F18 | 注销是自动的（hygiene 已验） | `pkill -x m1-probe` 后容器侧 `ls /sys/class/bluetooth` | 空；`rfkill list` 回到只有 `bt_power`+`phy0`，无残留 phantom |
| F19 | attach **必须 root**，但**容器不需要 root**：`TIOCSETD N_HCI` 对非 root 直接 EPERM，而安卓侧 `su`(KernelSU) 一发就过 | 容器内 `./m1-probe` vs `adb shell su -c` | 容器 uid1000 → `TIOCSETD N_HCI 失败: Operation not permitted`；安卓 su → OK。**→ 架构简化：attach+bridge 全程跑在安卓侧 root，容器只用现成的 root `bluetoothd`**（`busctl --system list` 显示 org.bluez 属主是 root） |
| F20 | 用户态 `hciconfig hci0 up` → EPERM；`bluetoothctl list` 在 hci0 存在期间**看不到** | 容器内实测 | ①上电要 CAP_NET_ADMIN → 只能由 root bluetoothd 做（不是问题，bluetoothd 本身就是 root）；②adapter 认领失败根因见 F22。 |
| F21 | **共享 netns 实锤（inode 级）** | 容器 `readlink /proc/self/ns/net` vs 安卓 `readlink /proc/1/ns/net` | 两边同为 `net:[4026531840]` → 同一 netns，**BT 套接字的 netns 归属不会成为隔离障碍**（也再次解释 5.23 改名事故为什么能打穿安卓） |
| F22 | ~~根因=容器没 udevd~~ **已作废，见 §24**：真因是 mgmt `Read_Index_List` 返回 0 个控制器 | | `pgrep -x systemd-udevd`=0；`/run/udev/control` 不存在；`nsenter -t <bluetoothd> -m -p -- /bin/journalctl -u bluetooth` → 重启后只有 `management interface 1.22 initialized`，**对 hci0 一字未提** | BlueZ `src/driver.c` 用 libudev 枚举 `subsystem=bluetooth`，而 `udev_device_new_from_syspath()` 依赖 udev 的**属性库 `/run/udev/data`**；udevd 没跑 → 库空 → 枚举到 sysfs 路径也解不出设备 → adapter 被**静默跳过**（无错误日志，符合实测）。推论：**DRM 模式下 desk-takeover 本来就会重启 udevd，这一环大概率自然通** → M1b 挪到真接管轮里验，不在 anland 常态赌共享 netns（虽然 `80-net-setup-link.rules` 掩码实测仍在位） |
| F14 | 共享 netns（容器与安卓同一网络栈） | 见 5.22/5.23（udev 改名事故同源） | 容器与安卓同 netns → unix abstract socket 天然互通；BT 套接字/netns 归属也不构成隔离 |
| F15 | HAL 客户端位**从 ksu 域可查到**（M0.1 前半已过） | `service check android.hardware.bluetooth.IBluetoothHci/default` | `Service ...: found`（`user`/`system` 反而 `not found` → 必须走 `su`，与 4.5 的期望一致） |
| F16 | HAL 是**最新 AIDL 版**，无 `<version>` 标签=跟随平台最新 stable | `cat /vendor/etc/vintf/manifest/bluetooth_hci.xml` | `<hal format="aidl"><name>android.hardware.bluetooth</name><fqname>IBluetoothHci/default</fqname>` 且 **没有 version 字段** → 取 AOSP `hardware/interfaces/bluetooth/aidl` 的 tip 生成桩；另有 `bttpi-saidl.xml`、`fm_hci.xml`、`bluetooth_audio.xml`、`bluetooth_sar.xml`（同族服务，本期一律不碰） |

**由此得到的核心判断**：这块芯片在软件上属于「CP 侧控制器 + 用户态栈」架构——安卓的 Fluoride 栈（`com.android.bluetooth`）通过 vendor HAL 经 glink 直接收发 HCI，内核蓝牙栈全程旁观。所以：

- **不能去抢硬件**（F3：HAL 持节点；F10：QCA 传输层协议已被删；F7：电源协调在 btpower/cnss 里）——任何抢法都要我们复现上电/下载补丁序列，这正是三次强制重启的成因。
- **可以抢"客户端位"**：DRM 接管时 `stop` 杀掉的是 Framework 和 `com.android.bluetooth`，而 HAL 本体（F4）活着。**HAL 的 HCI 客户端槽位空出来了**，我们去做它的客户端，电源、固件、glink 全部仍由 vendor 代码按设计处理，我们只搬 HCI 包。

---

## 2. 已排除的路线（逐条否因，防止后人重走）

| 路线 | 否因 |
|---|---|
| A. 编 `hci_qca.ko` 让内核直接驱动芯片 | hci_qca 的**对端不存在**：F2/F3 证明 HCI 不经 UART 而经 glink；就算装上，它和 vendor HAL 会同时抢同一块芯片 → 必然冲突。且固件删了它是有意的（F10）。 |
| B. 容器抢 `/dev/bt_cp_ctrl` 直发 HCI | F3：HAL 进程已持有该 fd。就算 glink_pkt 允许多开，包会被两个读者分走（HAL 也在 read）→ 双栈同时驱动链路层 = 状态机撕裂。且 HAL 一退场就没人负责上电，逼我们打 btpower/geni 电源 ioctl（红线，见 §6）。 |
| C. 抢 `/dev/ttyHS0` + `hciattach qca` | 同上双竞争；而且 `hci_qca.ko` 不存在 → `HCI_UART_QCA` 这个 line-discipline 协议也没了（F10），`hciattach qca` 直接失败。 |
| D. 自建 `hci_vhci.ko`（out-of-tree） | `CONFIG_BT_HCIVHCI is not set`（F9）→ 必须自己编模块。而 `CONFIG_MODVERSIONS=y` + `struct hci_dev` 布局必须逐字节对上真机源码；编错不是报错，是**踩共享内核的内存，连坐安卓**。收益（比 H4+pty 多一个 `/dev/vhci`）不值得这个风险。**否。** |
| E. 代理安卓 Framework 的蓝牙（`cmd bluetooth_manager` / 假 org.bluez 转发） | DRM 期间 system_server 被 SIGSTOP 冻结（这是躲 AOSP watchdog 杀核的前提，见工作总结 0.5/0.6），Framework 蓝牙服务不可用；解冻就会在 ~117s 被 watchdog 杀掉并级联炸网。结构性不可行。 |
| F. 进 DRM 前在安卓里配对好，指望设备继续可用 | 数据通路 `CP→glink→HAL→Fluoride→uhid→内核 evdev` 里 **Fluoride 是必经的搬运工**，它一冻结就断流（uhid 节点还在，但没有数据）。 |
| G. 蓝牙 USB/直通 | 芯片不是 USB 设备（PCIe/glink combo），`Droidspaces-USB-Manager` 那套直通帮不上。 |

---

## 3. 选定架构

```
        ┌───────────────── 共享内核（安卓与容器同一颗） ─────────────────┐
        │  hci_uart(H4)  →  hci0   →  bluetooth 核心  →  HIDP/BTHID      │
        │                                      ↑ evdev: /dev/input/eventN │
        └──────────────────────────────────────┼─────────────────────────┘
                                    pty slave  │                      │
                          hciattach            │                      ▼
        容器(与安卓共 rootfs；DRM 态下 anland 会话已被杀) ──┐          ┌────────┴────────┐      Plasma / kwin / libinput
   用户态 │ bluetoothd(org.bluez) ← bluedevil 原生 UI   │      （键鼠即插即用，无 uinput）
        │ bt-hci-shim (pty master ↔ unix socket)       │
        └──────────────────────┬───────────────────────┘
                     unix socket（共享 netns，abstract name "qoder-bt-hci"）
        安卓侧 │  bthci-bridge：IBluetoothHci 的 binder 客户端 + H4 组帧     │
        ┐      └──────────────────────┬──────────────────────────────────────┘
   HAL  │  android.hardware.bluetooth-aidl-qti（class hal，接管期唯一活着的蓝牙进程）
   芯片 ▼  glink /dev/bt_cp_ctrl + btpower(rfkill0) 上电/下载补丁  →  peach  combo 芯片
```

**为什么没有竞争者**：接管期 `stop` 之后，安卓 Framework 与 Fluoride 都不在了，HAL 处于「活着但没有客户端」状态。我们先 `svc bluetooth disable`（让安卓自己释放槽位），再 `initialize()` 顶上；回滚时我们 `disable()+close()` 退场，安卓 framework 重启后自己重新绑定。**全程只有单一 HCI 客户端、单一日志通路，电源动作 100% 由 vendor 代码完成。**

**为什么是"原生"**：hci0 是真·内核 adapter → 真 BlueZ → bluedevil/系统设置原样可用；HID 走内核 HIDP，键鼠以 `/dev/input/eventN` 出现在共享内核里，kwin/libinput 直接读——和我们已经趟通的 touch 事件节点是同一套路子，**不需要 uinput**（也就绕开了 uinput 防滥用拉黑整个会话的那个坑，工作总结 5.20）。

**吞吐评估**：HCI over binder 是「一包一次事务」。HID 场景包速率极低（键盘连打 <300 pkt/s，鼠标 <125 pkt/s），一次 binder 往返 ~10–50µs，余量充足。将来若做 A2DP（60–70 pkt/s ×100B 级别）同样够用。**不需要共享内存环形缓冲。**

---

## 4. 组件详细设计

### 4.1 `bthci-bridge`（安卓侧 binder 客户端，工作量主体）— **09-24 深夜定稿为 libbinder_ndk 版**

- **不需要 AIDL 代码生成**：`AServiceManager_getService(kSvc)` 拿代理 → `AIBinder_prepareTransaction(hal,&in)`
  让**平台自己**写 interface token 与 tuning header → 我们只 `AParcel_writeByteArray/writeStrongBinder`
  填参数 → `AIBinder_transact(hal, code, &in, &out, flags)`。方法码就是 AIDL 声明顺序（1 initialize
  … 7 sendScoData）。
- 回调端：`AIBinder_Class_define("android.hardware.bluetooth.IBluetoothHciCallbacks", create, destroy,
  onTransact)` + `AIBinder_new`，`onTransact` 里 `AParcel_readByteArray` 取 HCI 包写进 pty；
  `ABinderProcess_setThreadPoolMaxThreadCount(2)` + `startThreadPool()` 让回调在 binder 线程上跑，
  主线程只管 pty→HAL 的顺序转发（**顺序性由主线程保证，pty 写用 mutex 串行**）。
- 已判死的路（别再来）：手写 binder 事务。原因见 §11。`tools/bthci-bridge.c` 保留作 attach/H4 参考。
- 风险点（首次 CI 构建 + 首跑要验）：① 回调 binder 的 stability（platform-only 的
  `AIBinder_forceDowngradeToVendorStability` 用 `dlsym` 软调用，拿不到就跳过）；② `AParcel_readByteArray`
  的签名漂移（NDK 各版本参数略有差异，编译期即暴露）；③ HAL 是否接受"第二个 initialize"（M2 主验项）。
- **禁止清单**：不 open `bt_cp_ctrl`/`ttyHS0`、不打任何 btpower/geni 电源 ioctl、不写 `rfkill0`、
  不 `setenforce 0`。退场必须 `disable()+close()`，异常退出也要让 binder 死亡通知把 HAL 放回干净态。

### 4.2 attach 侧（M1 实测后大幅简化：**不再有容器 shim，也不需要 hciattach**）

09-24 M1 实测（F17/F19）推翻了原设计里"容器 root + hciattach + pty 胶水"这一整块：

- 容器侧 root 拿不到（`sudo` 要密码，且我们不愿把密码写进任何脚本/日志），但**根本不需要**：`hci_uart` 的 attach 只需要 `TIOCSETD N_HCI` + `HCIUARTSETPROTO(H4)` 两个 ioctl，**安卓侧 KernelSU root 一发成功**，hci0 立刻出现在**共享内核**的 `/sys/class/bluetooth`，容器侧 `hciconfig -a` 直接看得见（`Type: Primary / Bus: UART`）。
- 于是：**attach + pty master + binder 客户端 = 同一个安卓侧 root 进程**（`bthci-bridge`），它自己开 pty 对、自己在 slave 上挂 N_HCI/H4、自己拿 master 收发 H4 帧、自己直调 `IBluetoothHci`。**同进程内不需要任何 socket/跨界传输**，容器只保留现成的 root `bluetoothd` + Plasma UI。
- 参考实现已经写好并跑过：`tools/m1-probe.c`（静态编译、`adb push /data/local/tmp`、`su -c` 执行），它是 bridge 的前 60 行骨架。
- 清理语义已验（F18）：进程退出 → tty 关闭 → `hci_unregister_dev`，`/sys/class/bluetooth` 变空、rfkill 无残留 → **回滚路径天然干净**，desk-stop 只需 `pkill -x bthci-bridge`（**必须 `-x`：`pkill -f` 会连 `su -c` 的 shell 自己一起杀掉，实测踩过**）。
- 待解（M1b，F20）：hci0 存在期间容器 `bluetoothctl list` 看不到 → BlueZ 靠 udev 枚举 adapter；DRM 模式下 desk-takeover 本来就重启 udevd，预计自然成立，但要实测确认（必要时改成 hci0 就位后再 `systemctl try-restart bluetooth`，顺序写进 desk-takeover）。

### 4.3 内核/系统位

- 需要的内核能力**全部现成**：`hci_uart`+H4（F8）、`bluetooth` 核心（F1）、`hidp`（F1，BlueZ 的内核侧 HID 通道）。**不新增任何模块。**
- hci0 会出现在**共享内核**：安卓侧 `dumpsys bluetooth_manager` 之类不会主动用它（安卓栈走用户态，F2），但要按「新增全局设备」对待，列入回归清单（§7）。
- 若容器缺 `/dev/rfkill`（接管期 /dev 重建的历史坑），desk-takeover 已有 `mknod /dev/rfkill c 10 242` 段可直接复用（工作总结 5.17）。

### 4.4 Plasma/BlueZ 侧配置

- `bluetoothd` 已在跑且 `org.bluez` 在总线上（F11）→ bluedevil 侧零改动。
- 配对授权：容器无 logind active 会话，polkit 可能拒 `org.bluez.agent` / `ConnectDevice`（与 plasma-nm "Not authorized to control networking" 同因，工作总结 5.16）。对策同款式：接管期写 `/etc/polkit-1/rules.d/62-bluez-drm.rules` 放行 `xieyizhou` 的 `org.bluez.*`，然后 `systemctl try-restart polkit`。
- 首次接管后需要在 DRM 桌面里点一次「配对」，成功即持久化（BlueZ 自己存 `/var/lib/bluetooth`，HOME/系统盘都在容器里，跨轮次保留）。

### 4.5 SELinux（唯一可能需要"策略"的地方）

- 期望：**不需要**。容器进程读 binderfs 节点是既有事实（F12 的节点带 label 且 anland 图形栈一直在用 binder），`bthci-bridge` 以 `ksu` 域跑（KernelSU 的 su 域对 hal 客户端通常已放行）。
- 若 M0 探到 `avc: denied { call } ... name="vndservicemanager"` 或 `binder { call transfer }` 到 `hal_bluetooth_default`：
  - 走 **KernelSU 模块 `sepolicy.rule`**（`/data/adb/modules/droid-bt/sepolicy.rule`，由 ksud 在开机时打补丁；F13 显示本机 ksud 无独立 magiskpolicy，故**不自建 raw policy**），规则按 avc 实际字段逐条加，只加 `allow`，不上 permissive 域。
  - 模块形态同时承载 `bthci-bridge` 二进制与启动脚本；**`service.sh` 里只装文件、不自动起守护**（守护由 desk-takeover 触发），避免安卓侧凭空多一个常驻蓝牙进程。

---

## 5. 里程碑（每步都有判据、退路、风险）

### M0 · 决定性探测（只读，0 风险，anland 模式即可做）— **进行中：0.1 前半 + 0.2 已过（见 F15/F16）**

1. binder 可达性：`service check android.hardware.bluetooth.IBluetoothHci/default` ✅ **found**（F15）。**待做**：`service call android.hardware.bluetooth.IBluetoothHci/default 1`（首个用户事务，空参数预期回错误码，但能证明事务发得出去、且能看出是否被 SELinux 拦）。判据：拿到 binder 回复而非 `cannot find service` / `avc denied`。
2. ✅ 版本确认（F16）：**最新 AIDL、无 version 标签** → 用 AOSP `hardware/interfaces/bluetooth/aidl` tip 的 `IBluetoothHci.aidl` + `IBluetoothHciCallbacks.aidl` 生成桩；方法表含 v3 的 `configure/downConfigure`，事务码按该版本方法顺序走。
3. 观察基线：`ls -l /proc/$(pidof android.hardware.bluetooth-aidl-service-qti)/fd`（记 HAL pid 与 fd 表），并记录 `svc bluetooth disable/enable` 前后 rfkill0 的 `soft/state` 变化 → 确认「disable = 释放客户端槽位，但 HAL 仍持节点」这一关键假设。
4. 失败退路：若 binder 完全不通 → 本方案降级为 §8 的「降级形态」（容器内 btstack + uinput，仍需 HCI 来源，说明届时只剩「申请 KernelSU 域内做 binder 补丁」一条硬路），并重新评估是否值得做。
5. 风险：**无**（`service call` 只发一个非法/空参数事务，HAL 最多回一个错误）。

### M1 · 假 controller 打通内核侧 — **主体已过（F17–F20），剩 M1b**

- ✅ 已完成：`tools/m1-probe.c` 在安卓侧 root 下用 pty+N_HCI+H4 **注册出真 hci0**，容器侧 `hciconfig -a` 可见；退出后自动注销、无 rfkill 残留（回滚天然干净）。
- ✅ 已完成：证明"零新内核模块"成立，并因此把架构从「容器 shim + hciattach + 跨界 socket」简化成「安卓侧单进程 bridge」。
- ⬜ **M1b（挪到真接管轮做，根因已定位=F22）**：让容器 `bluetoothd` **认领** hci0。判据：`bluetoothctl list` 出现 hci0、DRM 桌面 bluedevil 图标从"无适配器"变可用。手段（按新证据排序）：① 直接靠 desk-takeover 已有的 `systemctl restart systemd-udevd`（udev 属性库一旦建起来，BlueZ 就能枚举）→ 顺序改成 **attach 造 hci0 → udevadm trigger --action=add --subsystem-match=bluetooth → 再 restart bluetooth**；② 若仍不认领，`bluetoothd -d` 抓日志看 `driver.c` 到底跳过了什么；③ 兜底=不依赖 BlueZ 的 udev 路径，改由我们自己在 desk-takeover 里 `udevadm trigger` 补事件。
- 🔧 **顺带记两条实操技巧**：以 root 进容器执行 = `nsenter -t <宿主机看到的 bluetoothd PID> -m -p -- /bin/systemctl ...`（**`-a` 会 `setns: Invalid argument`**，cgroup/user 两个 ns 进不去，只 `-m -p` 就够）；`pkill -f m1-probe` 会连 `su -c` 那层 shell 一起杀（rc=143 假失败），**必须 `pkill -x`**。
- ⬜ M1c（可选）：把 `m1-probe` 的假应答升级成完整假 controller（`hciconfig hci0 up` 走通 + 假 BD_ADDR + 假扫描响应），用来在没有真 HCI 的情况下把 bluedevil 的配对流程整个演一遍。
- 风险：共享内核多出一个 hci0（安卓侧 Fluoride 不用内核 BT，理论无感；已验退出无残留）。M1 全程**没碰芯片**，`bt_power` 保持 soft blocked。

### M2 · 真 HCI 接上（**全流程唯一有真机风险的一步**）

- 前置：`svc bluetooth disable`（用户已授权范围内；等价于在安卓开关里关掉蓝牙，可 `svc bluetooth enable` 复原，与重启无关）。
- bridge `initialize()+enable()` → shim 侧 `hciconfig hci0 up` → 判据：`hciconfig hci0` 读到**真实 controller MAC**（不是假回环写死的）、`bluetoothctl scan on` 能列出真实周边设备。
- 失败表现分三类，各自的处置：
  - `initialize` 被拒/无回调 → HAL 槽位未释放或版本方法表不对 → 回 M0.3 复查，必要时 `setprop ctl.restart vendor.bluetooth-aidl-qti`（class hal 重启，init 托管，安全）。
  - `enable` 卡住无 `transportReset/hciEvent` → 芯片未上电（HAL 的电源路径依赖 framework 的另一半？）→ **此时严禁手动补电**（红线），改为先 `svc bluetooth enable` 再 `disable` 走一遍 vendor 自己的上电/下电，让它把状态机留在「已初始化」上。
  - 上电过程拖垮系统（SSR/hangdetect，即历史三次强启的表现）→ 强制重启回安卓，回来读 `logs/harvest/` 的 dmesg 定位。**这是本步骤最坏结果，可接受但不预期**（我们全程没打过电源接口）。

### M3 · HID 端到端 + 生命周期接线

- 在 DRM 桌面里用 bluedevil 配一副蓝牙键盘 → 判据：`/proc/bus/input/devices` 新增该设备、`bin/touchtest` 之外的键盘输入在 Konsole 里可见（**注意：触摸节点禁 getevent 的规矩同样适用于这里旁听，验证一律看 kwin.log / input devices，不用 getevent**）。
- 接进 `desk-takeover.sh`：顺序 = 读安卓 SSID/PSK 那一段附近，`svc bluetooth disable` → 起 bridge（**在 `stop` 之前**，此时还能用 vndservicemanager 解析句柄；句柄一旦拿到与名字服务共存亡无关）→ 起 shim+hciattach → 等 hci0 → 起 NM/桌面。
- 接进 `desk-stop.sh`：`kill_desktop` 里加 bridge/shim 清理（TERM→3s→KILL，bridge 退出前必 `disable()+close()`），**必须在 `start` 之前**完成；再走既有的 udevd/wpa/改名自愈逻辑。
- 全部 `run()` 调用继续套 12s 限时；bridge 崩溃要能自愈（不阻塞回滚，接管保命优先于蓝牙）。

### M4 · 回归与交付

- 一轮完整「进 DRM → 用蓝牙键鼠打字 → 点返回安卓」，验证 §0 的 4 条验收（尤其第 3 条：回安卓后蓝牙开关/已配对设备/网络共享全好，**不重启**）。
- 独立成仓（§8）：`droid-bluetooth-bridge` 附 KernelSU 模块打包 + `README` 安装/回滚说明 + 主项目两处调用点的 PR 记录。

---

## 6. 红线（本方案内绝对不做）

1. **绝不从容器手动驱动蓝牙电源 ioctl**（`ttyHS0` 上的 `0xBFAC–0xBFAF`、`0x54ed/0x54ee`、geni grant `0xBFE4`）——三次强制重启的成因：peach 是 WiFi(PCIe)+BT combo，电源域由 `btpower.ko`/cnss 协调，绕过协调者 = SSR/hangdetect 复位。
2. **绝不在接管期 `dmesg -c`**（第三次重启前我清环，现场全灭）。取证只读 `dmesg`，收割靠 `scripts/dmesg-harvester.sh`。
3. **绝不 `setenforce 0`**（改整机安全态势，且会被小米安全组件感知的风险未知）；只允许最小 `allow` 规则。
4. **绝不 ptrace 活的 vendor HAL / composer**（会引入我们已经在 SF 上吃过教训的停顿语义）。
5. 触摸/输入节点**禁 getevent 旁听**（工作总结 4.4：触发彻底断网且只能强启）。
6. 不在容器里给蓝牙新增任何 systemd/KernelSU **自启**；组件生命周期严格挂在 desk-takeover / desk-stop 上。
7. 改 `desk-takeover.sh` / `desk-stop.sh` 时不在接管运行中编辑正在跑的副本。

---

## 7. 副作用与回归面（必须逐条验）

| 面 | 变化 | 回归动作 |
|---|---|---|
| 共享内核 | 接管期多出一个 `hci0` + 一个 rfkill/evdev | 回滚后 shim/hciattach 退出 → `hci_unregister_dev` 自动消失；desk-stop 增加 `ls /sys/class/bluetooth` 断言（非空则告警） |
| 安卓蓝牙 | 接管期我们坐它的 HAL 客户端位 | 回滚后 framework 重新 `initialize`；实测开关切换、重连已配对设备、BT 网络共享三项 |
| anland 模式 | 理论上零影响（组件不运行） | 跑一轮 anland：Droid Spaces 桌面正常、安卓蓝牙可用、容器 `bluetoothd` 不再报 "no adapter"（它会看到 hci0 消失，属正常） |
| WiFi | 同一颗 combo 芯片 | 每次 M2/M3 轮次后照常验 WiFi（udev 掩码 + 改名自愈那套，工作总结 5.23） |
| 电源 | 接管期芯片被点亮 | 长轮次观察电量/发热；`power-state-sync` 托盘显示不受影响 |
| 保命路径 | desk-stop 多了一个清理点 | **接管保命优先**：bridge/shim 任何异常都不得阻塞 `start`；已有限时+50s 看门狗照旧覆盖 |

---

## 8. 是否新开一个项目/仓库

**结论：值得独立成仓，但不要在 M0–M1 就分家。**

- 独立成仓的理由（三条都成立）：
  1. **产物形态不同**：主体是「一个安卓侧 native 守护 + 容器侧 shim + KernelSU 模块 + AIDL 生成桩」，构建链（NDK/aidl/模块打包/CI）与 DRM 主项目（shell + kwin 补丁）完全不同。
  2. **可独立回滚**：它是「借还硬件」的协议，坏了只坏蓝牙；但它是**唯一有可能把安卓拖到重启的组件**，所以必须能一键卸载/禁用，独立模块目录 + 独立版本最干净。
  3. **先例**：`droid-pc-keyboard` 拆出去之后 deb 化 + CI 很顺，主仓只留一个引用点。
- **【09-24 深夜更新：用户已决定建仓，仓名 `droid-bluetooth-bridge`（不用 bt 缩写）】** 目录已就地改名并同步全部引用；`src/` + `build.sh` + CI 骨架已就位，等一次 push 触发首次构建。
- 落地形态：主项目 `droid-drm-takeover` 只保留 desk-takeover/desk-stop 里的两个调用点；本目录独立 git init 为 `droid-bluetooth-bridge`（License 待定：主项目是 GPL-3.0、pc-keyboard 是 MIT，建议随主项目用 GPL-3.0，因为我们要复用 kwin 侧 GPL 代码？——**待用户定**），并照 pc-keyboard 的规矩**只在用户明确要求时推送 GitHub**。

---

## 9. 待你确认/需要你的地方

1. **M0 是否现在开跑**（只读探测，含一次 `service check/call`，不动任何状态）。
2. M2 需要你配合一次「安卓蓝牙关掉」级别的变更（`svc bluetooth disable`，可一键复原）——真 HCI 阶段前会再单独问一次。
3. 如果 M0 撞到 SELinux：是否允许新增一个 KernelSU 模块（`/data/adb/modules/droid-bt`，只加最小 `allow` 规则，不 permissive）？这会改启动期的策略，属于「影响面超出会话」的变更，需要你点头。
4. 一副可配对的蓝牙键盘/鼠标（M3 需要真设备；已有的话告诉我型号即可）。

---

## 11. 手写 binder 判死 + 改道决定（09-24 深夜，反汇编实锤）

**结论：不要手写 binder 事务，改走「NDK/libbinder_ndk + GitHub Actions 云端编译」。**

证据链（全部来自 `objdump` 反汇编设备拉回来的 `/system/lib64/libbinder.so`，缓存于本机 `/tmp/libbinder.so` + 全量反汇编 `/tmp/lb.asm`，下次别再拉）：

1. `getService` 一律回 **4 字节 `0x80000001`** = libbinder 的 `EXCEPTION_CODE|EX_SECURITY`；且 **dmesg 没有任何 `avc: denied { find }`** → 不是 SELinux 拒绝，是 **servicemanager 在解析 parcel 阶段就把我们当"另一份 libbinder"拒了**。
2. 该检查位于 **`Parcel::enforceInterface(char16_t const*, size_t, IPCThreadState*)`**（libbinder.so+0x8cd2c）：现场构造 `0x53595354`（'SYST'）比对，vendor 侧对应 `0x564e4452`（'VNDR'）——就是 dmesg 那句 `Expecting header ... before position 12` 的出处。
3. 这个 header **不是 prepareTransaction 写的**：`AIBinder_prepareTransaction` 只做 `Parcel::markForBinder(target)`，而 `markForBinder` 也只是**读目标 binder 的 stability 标志（+0x20==1）给 parcel 打个内部标记**；真正把 header/`Tuning` 块序列化进 parcel 的是 **`Parcel::write`**（一个 0x2e40+ 字节的大函数，里面还有 `_PDI/_PGN/_SRD/EDR` 一类 fourcc 的 tuning 分支）。
4. 我们试过的 23 种组合（事务码 1–8 × header 排布 5–6 种 × 带/不带 token，vndbinder/binder 两个节点）**全部同样 4 字节 EX_SECURITY**，且码 1..8 回包尺寸完全一致 → 说明 `getService`/`listServices` 等根本没进到参数解析。**要继续就得把 `Parcel::write` 的 tuning/header 语义整个逆向出来**——性价比明显不划算，而且它随 Android 版本变。

所以：`tools/bthci-bridge.c` 保留为 **attach/H4/pump 的参考实现与调试工具**（其中 pty+N_HCI+H4、BC/BR 收发循环、mmap 规则、`+4` 偏移这些坑都已趟平并有注释），但 **binder 客户端那一层换成平台自己的 `libbinder_ndk`**：

- 本机**编不出** bionic 目标：没有 NDK；NDK 只有 x86_64 host 版（这台是 arm64）；`~/android-sdk` 只有 cmdline-tools；**连 java/javac 都没有**（Java + `app_process` 那条路也暂时被工具链卡住）。glibc 静态二进制能跑，但 glibc 静态**不能 dlopen**，而且 bionic 的 .so 也解析不了 glibc 符号 → 别想着运行时加载 `libbinder_ndk`。
- **可行且最省**：GitHub Actions 跑 `android-ndk-r27` 编译（`aidl --lang=ndk` 生成 `IBluetoothHci`/`IBluetoothHciCallbacks` 桩 + `-DBIONICTEST` 无关，链接 `libbinder_ndk`、`c++_static`），产物 artifact 下载后 `adb push /data/local/tmp` 直接跑。`droid-pc-keyboard` 已验证我们的 CI 链路可用（含国内网络下取 artifact 的套路）。
- 需要用户点头的两件事：① 在**现有** `droid-drm-takeover` 仓库加一条 workflow（不必新开项目），或同意为 `droid-bluetooth-bridge` 建独立仓；② 允许一次 `git push`（按规矩只在明确同意时推）。

顺带存下 M2 前必须记住的两条：`initialize`/`enable` 走 `libbinder_ndk` 之后就不该再有 EX_SECURITY；以及 **`p_local_binder` 里我们自己造的回调 binder 也要换成 NDK 的 `AIBinder_Class_define` + `ndk::ScopedAIBinder_Weak`**，否则平台侧仍可能因 stability 不认。


## 10. 当前进度与卡点（09-24 深夜，写给下一次会话）

`tools/bthci-bridge.c` 现状：**静态编译通过、安卓侧 root 可跑、binder mmap 注册成功、同步事务被 servicemanager 正常应答、不再 SIGSEGV**。剩余卡点=拿不到服务句柄。

已趟掉的四个真坑（都已修进代码，别回退）：
1. **`read_size=0` 会让内核把事务当异步**（`binder.c` 用 `!!wr.read_size` 决定要不要回包）→ 同步调用必须把 write+read 放进**同一次** `BINDER_WRITE_READ`，否则只能收到 `BR_TRANSACTION_COMPLETE`(0x7206)。
2. **必须 `mmap(fd, 1MB, PROT_READ, MAP_PRIVATE)`，且绝不能带 `MAP_ANONYMOUS`**（带了内核不走 `binder_mmap`，进程没有 allocator → `binder_alloc_buf, no vma` → `BR_DEAD_REPLY`）。
3. **incoming 的 payload 不在 read buffer 里，而在那块 mmap 区**：`td->data.ptr.buffer` 本身就是可用用户指针；按 `g_rbuf` 算偏移直接 SIGSEGV。
4. **命令字后面要 `+4` 才是结构体**：`p` 指着 `BR_*` 命令本身，必须 `(struct binder_transaction_data *)(p + 4)`；差这 4 字节会让全部字段看起来是垃圾（为此绕了一整轮）。

未解问题（按可疑度排序）：
- **A. 更可能是"我们把回包读错了"而不是"服务找不到"**：各种事务码 × 各种 12 字节 header 排布试了 30 次，回包**恒为 4 字节**。但返回 null binder 的合法回包至少是 `EX_NONE(4) + BINDER_TYPE_NONE(24) = 28` 字节；**4 字节恰好只对应"无异常 + void 返回"** → 说明我们可能根本没在 `IServiceManager` 上调用到 getService，或 `data_size` 字段位置仍未对本机内核对齐（本机 raw 流是 `BR_NOOP(4) BR_TRANSACTION_COMPLETE(4) BR_REPLY(4)+64B`，与 UAPI 64 字节版一致，但数值仍存疑）。
  - **一次就能判别的实验（下一步就做）**：用**确定存在的 framework 服务名**（`bluetooth_manager` / `activity`）走完全相同的 getService；回包同样恒 4 字节 → 解析问题（去核对 `data_size`/`offsets_size` 的真实偏移，必要时逐 8 字节扫出 `BINDER_TYPE_HANDLE=0x73682a` 特征）；出现 28/32 字节 → 才是 `IBluetoothHci/default` 不在这个 servicemanager 的命名空间里。
  - 另有待验证项：Android 16 的 libbinder 会对事务 parcel 做 **12 字节 header 完整性检查**（dmesg：`Expecting header 0x564e4452('VNDR') / 0x53595354('SYST') ... before position 12`，vendor/system 两份 libbinder 各认一个 fourcc）。非 libbinder 客户端要不要补这个 header 尚未定论（补 5 种排布都没改变 4 字节回包），代码里 `p_header(variant)` 已留着，随时可续试。
- B. `attach_hci()` 里 `HCIUARTGETDEVICE` 返回 -1 是假象（m1-probe 已证 hci0 确实注册成功），别拿它当判据，判据只看 `/sys/class/bluetooth`。
- C. 注意 `--find-code` 那条路径有 `goto`，会**跳到 attach + initialize 之后再跑探测**，于是日志里的 `initialize → rc=0` 是打给 **servicemanager(handle 0)** 的，不是 HAL——别误读成"HAL 已接通"。

M2 前置事实（本轮新证）：`lsof` 确认 `bt_cp_ctrl`/`ttyHS0` 由 HAL 进程(2198)持有且随蓝牙开关重开；`service call` 从 `u:r:ksu:s0` 可正常调用该 HAL 并拿到 void 回包。


---

## 12. 断点续做（09-24 收尾快照，重启平板后从这里接）

### 已完成并落盘
- **M0 通过**：`service check/call android.hardware.bluetooth.IBluetoothHci/default` 从 `u:r:ksu:s0` 可达（HAL pid 2198 无异常、无 avc `find` 拒绝）。
- **M1 通过**：`tools/m1-probe.c` 用 pty + `TIOCSETD N_HCI` + `HCIUARTSETPROTO(H4)` **在共享内核注册出真 `hci0`**，零新内核模块；进程退出即自动注销（`/sys/class/bluetooth` 回空、rfkill 无残留）。
- **手写 binder 判死**（原因+证据见 §11，四个硬坑见 工作总结 5.26）。
- 新仓已建并推送：**https://github.com/Yizhou147/droid-bluetooth-bridge**（public，MIT，与其余 droid-* 一致）；
  分支 `main`，两个提交 `ca69744`（骨架）+ `e3cf170`（MIT）。本地目录 `~/Documents/XiaomiPad8Pro-drm-display/droid-bluetooth-bridge`。

### 第一件事：修 CI（run 35990896288 失败在"安装 NDK r27"）
- 报错：`Warning: Failed to find package 'ndk;r27.2.12479018'` → **不是编译问题，是 sdkmanager 索引没刷新**。
- 修法（按优先级试）：① 直接用 runner **预装** 的 NDK：`ls /usr/local/lib/android/sdk/ndk`，能拿到就把安装步骤整段删掉、只 `export ANDROID_NDK_HOME=...`；② 若必须装：先 `sdkmanager --update`（加 `--verbose`）再 `sdkmanager "ndk;r27.2.12479018"`；③ 退一步用 `ndk;26.1.10909125` 之类确定存在的版本。
- 注意：首次 `push` 没自动触发 workflow（`on: push` 的 paths 命中了但没跑），**用 `gh workflow run build-bthci-bridge` 手动触发**是可靠的。
- 验证链：`gh workflow run` → `gh run list` → `gh run view <id> --log-failed` → `gh run download <id> -n bthci-bridge-aarch64`。

### 第二件事：M2 首跑（CI 出产物后）
判据与顺序（每条都是单独一行，可直接执行）：
1. `adb push out/bthci-bridge /data/local/tmp/bthci-bridge`
2. `adb shell su -c 'chmod 755 /data/local/tmp/bthci-bridge'`
3. `adb shell su -c 'settings get global bluetooth_on'`（记下当前值，便于复原）
4. `adb shell su -c 'svc bluetooth disable'` ← 需要你在场时做（等价于关安卓蓝牙开关，可 `svc bluetooth enable` 复原）
5. `adb shell su -c '/data/local/tmp/bthci-bridge --keep 60'`
6. 另开终端：`adb shell su -c 'ls /sys/class/bluetooth'` → 期望 `hci0`
7. 看 bridge 日志：`✓ 拿到 …`、`initialize → 0`、`enable → 0`

三个首跑要验的风险（§4.1）：回调 binder 的 vendor-stability、`AParcel_readByteArray` 签名、HAL 是否接受第二个 `initialize`。
最坏结果：蓝牙卡住 → 重启回安卓即恢复（我们全程不碰电源 ioctl，不会伤硬件）。

### 第三件事：M1b（bluedevil 认领 adapter）
根因已定位（§1 的 F22）：容器没跑 udevd，BlueZ 靠 udev 属性库枚举 adapter。
**必须在真接管轮里验**（desk-takeover 本来就重启 udevd），不要在 anland 常态赌共享 netns。

### 设备当前状态（无需清理）
`/sys/class/bluetooth` 空、无 `bthci-bridge`/`m1-probe` 残留进程、安卓 `bluetooth_on=1`、
`rfkill0(bt_power) soft=1`、WiFi/蓝牙全程未被改动。重启后以上全部自然归零。


## 13. M2 突破记录（09-24 深夜，实测数据）

**已通**：
- `AServiceManager_getService` 运行时可取句柄；但 `AIBinder_prepareTransaction` 对
  **没有 class 的裸句柄**一律 `-38(INVALID_OPERATION)`（descriptor 是从 class+0x48 取的）→
  **必须 `AIBinder_associateClass(g_hal, hciCls)`**（该符号设备 libbinder_ndk.so 导出，
  但要用 dlopen 的 handle 去 dlsym，`dlsym(RTLD_DEFAULT)` 查不到）。
- `initialize(callbacks)` = **事务码 1，同步**，回 `EX_NONE` ⇒ **HAL 客户端位已可被容器侧占据**。
- `enable(EnableReason)` = **事务码 4 + int32**（蓝牙关闭的干净状态下 `--sweep` 实测：
  code4+int32 使 HAL 的 `bt_cp_ctrl`/`ttyHS` fd 从 0→2）。
- 对远端句柄调 `AIBinder_forceDowngradeTo*Stability` 会
  `LOG_ALWAYS_FATAL("Can only downgrade local binder")` 直接把进程打死；降级只能用于本地 binder。
- oneway 调用的 `st=0` **只代表投递成功**，服务端可能因参数形状不对静默拒
  （logcat 里会留 `oneway function results for code N ... status NOT_ENOUGH_DATA/BAD_TYPE/
  ReadAndValidateArraySize`）——这就是判据要用「HAL 是否开传输 / rfkill 是否解除」的原因。

**下一步（M2 收尾，按优先级）**：
1. **时序**：`enable(4, reason)` 之后 HAL 会做 IBS DeviceWakeUp，约 1~2s 内等不到 HCI 就
   `pthread_cond_timedwait` 超时并被 SIGKILL（`libc: kill: send 9 to pid 2198`，实测 pid
   2198→17985 重启过）。所以必须 **enable 后立刻让内核开口**：
   `hciconfig hci0 up`（内核发 HCI_Reset → 桥转给 HAL）。root 进容器执行：
   `nsenter -t <容器bluetoothd宿主机PID> -m -p -- /usr/bin/hciconfig hci0 up`。
   更稳的做法是把 up 放到 enable **之前**（内核在 open 时才发 reset，桥先把命令缓存/排队）。
2. **定死 sendHciCommand/sendAclData/disable/close 的码位**：判据换成
   「发出一个合法 HCI_Reset 字节流后，IBS 超时是否停止 + 是否有 `hciEvent` 回调回来」。
   `--sweep` 已经能自动报 fd 变化，加一条"回调计数增长"判据即可。
3. HAL 一旦被 SIGKILL，我们手里的句柄失效（后续调用 `-32`）→ 桥必须检测 binder 死亡并
   重新 getService+associate+initialize，或干脆在 desk-takeover 里保证"先 up 再 enable"。
4. M1b 仍未做（容器 bluetoothd 认领 hci0 需要 udevd，见 F22），要在真接管轮里验。

**设备状态**：测试后已 `svc bluetooth enable`，`dumpsys bluetooth_manager` = `State: ON`，
`/sys/class/bluetooth` 无残留 hci0，无桥进程。


## 14. --auto 首跑结果（09-24 深夜 20:4x，写给下一次）

**新通的两环**：
- `kickHci(1)` 用 `nsenter -t <容器bluetoothd宿主PID> -m -p -- /usr/bin/hciconfig hci0 up`（**放后台**，
  它会阻塞等 init-complete）→ 返回 0，**内核确实把 HCI_Reset 发到了 pty 上**（日志 `转发=1~2`）。
  自建的"未绑定控制 socket + HCIDEVUP"是 **ENODEV**，别再用。
- `enable` = 码 4 + int32 已在上文实测确认（HAL 传输 fd 0→2）。

**仍未通**：`cmdCode ∈ {2,3,5,6,7,8}` 逐个试过后 **没有任何 hciEvent 回来**（`收回=0`）。
两个未排除的原因，按优先级：
1. **enable 其实又失效了**：--auto 里没有再查 HAL 的 `bt_cp_ctrl/ttyHS` fd 与 HAL pid
   （HAL 被 SIGKILL 后 pid 会变，句柄全废）。→ 下版把 `halTransportFds()`+`halPid()` 打进每个候选的日志行。
2. **byte[] 的负载形状不对**：我们现在把**含 H4 类型字节**的整帧塞进 `sendHciCommand`
   （`g_include_type=true`）。若该 HAL 要的是**不含类型字节**的裸 HCI 包，服务端就会静默丢包。
   → 下版让 --auto 对每个 cmdCode 同时试 `include_type = true/false` 两遍（二维扫，12 组）。

**下一次直接做的事**（代码已就位，只差这两处改动）：
1. 在 `--auto` 的日志行加 `HALfd`/`HALpid`，并比较前后 pid 判断是否被杀；若被杀，
   说明"enable→内核开口"的 1~2s 窗口还是太慢，需要在 enable **之前**就把 hci0 up 起来
   （让内核的 Reset 先堆在 pty 上，enable 后立刻转出去），或桥自己缓存首帧。
2. 把 `include_type` 纳入 --auto 的搜索维度。
3. 判据再加一条：HAL 日志里 `ibs_handler DeviceWakeUp ... timed out` 是否**停止刷屏**
   （停止=芯片真的在讲话了）。

**设备状态**：`svc bluetooth enable` 已做，`State: ON`，容器 `/sys/class/bluetooth` 空，无残留进程。


## 15. --search 二维扫结果与**唯一剩下的疑点**（09-24 21:0x）

14 个组合（cmdCode 2..8 × 负载含/不含 H4 类型字节）**全部 `收回=0` 且 `HALfd=0`**，
且这一整轮 **HAL pid 始终是 17381 没被杀**（说明芯片压根没被点着，也没有 IBS 超时刷屏）。
转发侧没问题：`hciconfig hci0 up` 之后内核确实把 HCI_Reset 送到了 pty（`转发=1~2`）。

⇒ 也就是说 **`enable` 这一轮根本没生效**，与 §13/§14 里"code=4+int32 使 fd 0→2"矛盾。
矛盾的来源已经查清一半：**那两次 fd 变化都是在"一次 acquire 之后连发多种事务码"的 sweep 里
观察到的**，而 fd 检查只在 int32/byte[] 两种形状后做，间隔 0.35s，芯片上电本身要 ~1s，
所以 **功劳可能属于前一个调用**（20:31 那次 sweep 里，`code=2/none` 之后 1.3s 才看到 fd=2 ⇒
**很可能 enable 其实是 code 2 且无参**，即 AOSP 原序 initialize=1/enable=2，
而 §13 里我把 `BluetoothHci::close()` 那行日志错认成"code 2=close"——那行其实是
**上一个进程退出时的 binder 死亡清理**，我在 §13 后半已经怀疑过这一点）。

**下一次必须做的实验（设计要点，别再犯同样的错）**：
1. **一次 acquire 只发一个 enable 候选**（一因一果，别连发），候选集
   `code ∈ {2,3,4} × arg ∈ {无参, int32=1, int32=0}`，共 9 组，每组之间
   `pkill` 重启 HAL 或确认 pid 未变，避免脏状态。
2. 判据只用 **`ls -l /proc/<HALpid>/fd | grep -cE 'bt_cp_ctrl|ttyHS'` 从 0 变 >0**，
   轮询 10 秒（上电慢，1s 采样一次），**不要再用日志行**推断。
3. **enable 生效之前不要 `hciconfig hci0 down`**：本轮每都 down 一次可能把 hci_uart 的
   ldisc/状态搞脏；只 up 不 down，或者干脆把 up 挪到 enable 生效之后再做。
4. 找到 enable 之后，再用"收回>0"定 `sendHciCommand` 码位（那一层判据是可靠的）。

代码位置：`src/bthci-bridge.cpp` 的 `--search`（改成单发候选矩阵即可）、
`acquire()`、`halTransportFds()`、`kickHci()`。设备状态已复原（蓝牙 ON，无残留）。


## 16. 更正 + 下一步（09-24 21:2x，控制变量实验的结论）

**撤回 §13 的结论**：`enable = code4 + int32` **不成立**。用"一次 acquire 只发一个候选"的
矩阵（code ∈ {2,3,4} × arg ∈ {无参, 1, 0}，共 9 组，每组轮询 7 秒看 HAL 的 `bt_cp_ctrl/ttyHS`
fd 是否从 0 变正）**全部失败**，且这期间 HAL pid 一直是 17381 没重启 ⇒ 判据可信。
§13 那次看到的 fd 0→2 应归因于安卓侧自己关蓝牙的收尾动作，与我们的调用无关。

**同一轮拿到的对照基准**：`svc bluetooth enable` 之后，同一个 HAL 进程（pid 不变 17381）
**fd 数变成 2**，logcat 出现完整的 `ibs_handler: ProcessIbsCmd: Received IBS_WAKE_IND` /
`IBS_SLEEP_IND` / `SerialClockVote: vote for UART CLK ON|OFF` / `wake_lock Acquire/Release`
—— 即安卓自己能把芯片驱动起来，HAL 不挑进程，只挑**调用方式**。

**矩阵漏掉的一个关键维度（下一次就做这个，成本 1 次构建）**：
9 个候选全都用 `FLAG_ONEWAY` 发送。而更早的日志里出现过
`oneway function results for code 3/4 ... will be dropped but finished with status ...`
—— **服务端给"我们标记为 oneway"的调用回了包**，说明这些方法其实是**同步（非 oneway）**的；
对同步方法发 oneway 调用，服务端可能直接按错误处理（BAD_TYPE/NOT_ENOUGH_DATA）而不执行。
而 `initialize` 正是因为我们用**同步**发才拿到 EX_NONE。

⇒ 下一次：同一矩阵改成 **`flags=0` 同步发送并读回包首 int32**：
  - `EX_NONE(0)` = 命中且执行；
  - `NOT_ENOUGH_DATA/BAD_TYPE` = 方法对但参数形状错；
  - `UNKNOWN_TRANSACTION(-38/-ENOSYS)` = 码位不对。
候选仍是 `{2,3,4,5} × arg ∈ {无参, int=1}`，每组照常轮询 HAL fd 作第二判据。

代码：`--search` 里把 `FLAG_ONEWAY` 改成参数开关即可（`acquire()`/`halTransportFds()` 都现成）。


## 17. ★ 真上电入口已确认：`initialize_aidl(callbacks)` = 事务码 2（同步）

`--search4` 实测（安卓 framework 全程 stop/蓝牙关闭状态）：

```
S4 code=1 sync   带回调 → st=0  HALfd=0
S4 code=1 oneway 带回调 → st=0  HALfd=0
S4 code=2 sync   带回调 → st=0  HALfd=2 ★★★
```

HAL 自己在同一时刻打的日志（21:13:49）就是铁证：

```
BluetoothHci: Using initialize_aidl.
BluetoothHci::initialize_aidl()
initialize_aidl: linking to deathRecipient
uart_transport: OpenUart: succeed to open /dev/ttyHS0, fd: 16
```

⇒ **QTI 在 AIDL 接口里加了 `initialize_aidl`，它才是上电+开传输的那一个**；老 `initialize`
（我们一直发的 code 1）回 EX_NONE 但什么都不做。之前所有"enable 候选矩阵"失败的原因也清楚了：
我们在找一个不存在/不干活的方法。**§13/§16 里关于 enable 的推断全部作废，以本节为准。**

**剩下的唯一问题**：`sendHciCommand` 码位没扫出来（S4b 里 cmdCode 1..8 全 `收回=0`）。
怀疑是 S4b 的循环设计问题，不是方法表问题：
1. 循环里每组都 `hciconfig hci0 down` 再来，但**没有重新 `initialize_aidl`** —— 第一组之后
   HAL 的传输可能已被 down 流程关掉，后面几组等于在对着一个没初始化的 HAL 发包。
2. 修正版：每个候选都走完整序列 `acquire() → initialize_aidl(2,sync,带回调) → hciconfig up（只 up 不 down）
   → pump 1~4 秒 → 看收回`；并且把 `include_type`（负载含/不含 H4 类型字节）也当维度。
3. 判据再加一条：HAL 日志是否出现 `ProcessIbsCmd: Received IBS_WAKE_IND` 之类的**芯片活动**。

代码：`--search4` 的 S4b 段（改成每组重新 acquire+initialize_aidl、去掉 down）。

**设备状态**：`svc bluetooth enable` 已做，`State: ON`，无残留桥进程。


## 18. S5/S6 两轮探针的教训 + 当前确切状态（09-24 21:4x）

**已确证可复现**：`initialize_aidl(callbacks) = 事务码 2、同步` → 每一轮 `HALfd` 都稳定变成 2
（UART/glink 已开、芯片已上电）。**方案的核心假设（做 HAL 客户端而不是抢硬件）到此完全成立。**

**两轮探针的方法学错误（别再犯）**：
1. **S5 用 oneway 探测**：oneway 拿不到任何服务端错误，`st=0` 只代表排队 ⇒ 全 0 是必然，无信息量。
2. **S6 的"命中"不可信**：`code=1 同步 + byte[]` 回了 `EX_NONE`，但 code 1 是老的 `initialize`
   （QTI 里它只是把回调存起来，不做参数校验也可能回 0）⇒ **EX_NONE 不等于"这个方法就是它"**。
   教训：**判据必须用副作用**（`HALfd` 变化 / 芯片真的回 event），不能只用 binder 返回值。
3. `hciconfig hci0 up` 目前仍是 `DOWN`+全零 MAC：内核那侧 init 超时（10s），因为
   我们**没有真的把 HCI_Reset 转出去并让芯片回 Command_Complete**——也就是
   `sendHciCommand` 码位仍然未知。

**下一次的确切做法（一次就能定）**：不再猜码位，**直接读 Fluoride 怎么调**。
`system/gd/hal/hci_backend_aidl.cc` 是调用方，它按 AIDL 生成顺序调用；而 HAL 日志已经告诉我们
`initialize_aidl` 存在（=码 2）。所以本机 AIDL 声明顺序应当是：

```
1 initialize(callbacks)          // 兼容遗留：只存回调
2 initialize_aidl(callbacks)     // ★ 真正上电（实测）
3 enable()                       // 待验：可能是 oneway
4 disable()                      // 待验
5 close()                        // 待验
6 sendHciCommand(in byte[])      // 待验
7 sendAclData(in byte[])
8 sendScoData(in byte[])
9 configure(...)  10 downConfigure(...)
```

判据设计：**发一条 `HCI_Reset` 后，看内核 hci0 会不会在 10 秒内变成 `UP` 且 `BD Address` 变真**
（=整条闭环通了），或至少 `g_cbEvents>0`。用 code 3..10 逐个**同步**试，但**只认 event/UP 这类副作用**，
不再把 `EX_NONE` 当结论。另外把 `include_type` 两值都试。


## 19. --why 结果：方法表基本成形，缺的是"上电"那一步（09-24 21:4x）

同步探测 + 回包解码（`--why`，码 1..12，负载 = 4 字节 HCI_Reset）：

| 码 | 结果 | 解读 |
|---|---|---|
| 1 | `-32`，0 字节 | 老 `initialize(callbacks)`：吃 binder，我们的 byte[] 让它挂了（**EX_NONE 不可信这条再次确认**） |
| 2 | 已知 = `initialize_aidl(callbacks)`，**oneway**；每轮都把 `HALfd` 顶到 2，HAL 打 `OpenUart: succeed to open /dev/ttyHS0, fd:16` + 大量 `wake_lock Acquire/Release` | **UART 开了、有活动** |
| 3、5 | **`st=0`、回包恰好 4 字节**（只有 EX_NONE，void 方法） | 收 `byte[]` 的 void 方法 ⇒ `sendHciCommand`/`sendAclData` 家族（4、6 未跑完被超时截断） |
| 7..10 | **`-74`(=BAD_TYPE)、0 字节** | **这些码位根本不存在** ⇒ 方法表到 6 为止 |

⇒ 本机 `IBluetoothHci` 只有 **6 个方法**：`1 initialize / 2 initialize_aidl / 3..6 = enable? disable? sendHciCommand / sendAclData / sendScoData` 的组合（oneway 与否还没分清）。**没有第 7 个"power on"** —— 也就是说**上电必须在 3..6 里面**，而我们一直没找对那一个。

**下一轮唯一正确的探针（这次别再自己骗自己）**：HAL 是**啰嗦的**（我们已经两次靠它的日志破案）。所以
**每发一次调用，就把该时刻之后 HAL pid 的日志行全捞出来**，看它自己打出的是
`BluetoothHci::enable()` / `HciHal::enable` / `DataHandler::Cleanup` / `PowerController: PowerOn` 中的哪一个 ——
**用方法名而不是返回值来定位**。实现：调用前记下 `date`，调用后 `logcat -d | grep <HALPID> | tail`。
候选仍是码 3..6 × `{无参, byte[HCI_Reset], int32}` × `{sync, oneway}`，
判据三条并列：**HAL 日志出现 enable/PowerOn 字样** + `rfkill0.soft` 变 0 + 回调收到 hciEvent。

**设备状态**：已 `svc bluetooth enable`，`State: ON`；HAL 进程pid 17381→18218（期间自己重启过一次，正常）。


## 20. 真正的缺失环节：QTI 的 **BtTpi** 电源接口（09-24 21:5x，离线扒依赖得出）

`--name` 跑完 24 组（码 3..6 × 无参/byte[]/int32 × sync/oneway）：
- **HAL 对这些调用一条方法名日志都不打**（`grep aidl_service` 全空），只有 IBS/wake_lock 活动；
- 期间还看到 **IBS 握手成功**（`DeviceWakeUp: Writing IBS_WAKE_IND` → `Received IBS_WAKE_ACK` →
  `Unblocked ... ret = 0`）⇒ 芯片醒着、UART 通，但 **HCI 层没有任何回应**；
- 结论：`IBluetoothHci` 的 6 个方法里**没有"上电/下载补丁"**那一步；HAL 只是开了传输。

扒 HAL 二进制的依赖（`readelf -d /vendor/bin/hw/android.hardware.bluetooth@aidl-service-qti`）看到：

```
NEEDED android.hardware.bluetooth-V1-ndk.so
NEEDED android.hardware.bluetooth@1.0-impl-qti.so
NEEDED android.hardware.bluetooth@aidl-impl-qti.so
NEEDED vendor.qti.hardware.bttpi-V3-ndk.so        ← 关键
```

`/vendor/etc/vintf/manifest/bttpi-saidl.xml` 给出服务名与版本：

```
name = vendor.qti.hardware.bttpi   version = 3   fqname = IBtTpi/default
```
（源路径 `vendor/qcom/proprietary/bluetooth/hidl_transport/bttpi/default/` —— 闭源）

⇒ **安卓 Fluoride 是通过 `vendor.qti.hardware.bttpi.IBtTpi/default` 给芯片上电/做状态切换的**，
不在 `IBluetoothHci` 里。我们要补的就是这一步：`initialize_aidl(2)` 之后，再调 BtTpi 的状态切换。

**下一步（用平台工具，零新代码，anland 模式即可测）**：
1. `service check vendor.qti.hardware.bttpi.IBtTpi/default` —— 确认服务在（`service list` 里没列，
   可能在 vndservicemanager 里）。
2. 用 `service call vendor.qti.hardware.bttpi.IBtTpi/default <code>` 逐个事务码试
   （**`service call` 自己会做 AIDL 编组**，正好绕开我们手写 binder 的一切麻烦），
   判据只看**副作用**：`dmesg | grep bt_power_vote` 是否出现 `BT powered ON`、
   `/sys/class/rfkill/rfkill0/soft` 是否变 0、HAL 日志是否出现 `state_switch_on` 之类字样。
   （BtTpi 的方法名候选：`init` / `state_switch_on` / `state_switch_off` / `updateParam` / `getBtState`。）
3. 找到"上电"那一发之后，回到 `IBluetoothHci`：`initialize_aidl(2)` → BtTpi 上电 →
   `hciconfig hci0 up` → 内核 HCI_Reset 经 `sendHciCommand`（码 3..6 之一，届时用"芯片回 event"定）
   → `hciconfig -a` 读到真实 BD_ADDR ⇒ M2 通。

**安全边界**：BtTpi 是电源相关接口，但它是**安卓自己每天在用的正常入口**（不是我们之前禁掉的
裸 `btpower` ioctl），所以允许试；每轮结束一律 `svc bluetooth enable` 回到安卓正常态，
不留下"我们点着但没人管"的芯片状态。

**设备状态**：`svc bluetooth enable` 已做，`State: ON`。


## 21. BtTpi 首轮试探（`service call`，anland 模式，09-24 21:43）

服务确实存在：`Service vendor.qti.hardware.bttpi.IBtTpi/default: found`。
无参调用各事务码的结果（判据 `dmesg|grep -c bt_power_vote` 全程 **170 不变** ⇒ 没触发电源投票）：

| 码 | 回包 | 含义 |
|---|---|---|
| 1..4 | `Error 0xffffffc3 "No data available"` | **方法存在但要求参数**（我们在蓝牙关闭状态下调，没有副作用） |
| 5 | `0x80000001` = `EX_SECURITY` | 存在但被权限/状态拒绝（值得注意：可能要求"BT 已开"才允许调） |
| 6 | `00000000 00000001` = EX_NONE + `1` | 一个**返回布尔**的查询方法（像是 `isXxxSupported`/`getBtState`） |

⇒ 下一步就是给 1..4 补参数形状：`service call … <code> i32 0` / `i32 1` / `s16 ""` /
`i32 0 i32 0`，每次只看三个副作用：`bt_power_vote` 计数是否增长、`rfkill0.soft` 是否变 0、
HAL/BtTpi 自己打了什么日志（`logcat -d | grep -i bttpi`）。
**注意**：BtTpi 是电源相关接口，每轮测完必须 `svc bluetooth enable` 复位（本轮已复位，`State: ON`）。
另外 code 5 的 EX_SECURITY 提示：某些方法可能要求"安卓自己已经开过蓝牙"才放行，
所以也值得在 **BT 开着**的状态下再扫一遍 1..4 做对照。


## 22. BtTpi 路线判死（模块 v0.1→v0.3 三轮实测），转 B 路线

| 轮次 | 加的策略 | 结果 |
|---|---|---|
| v0.1 | `ksu` 域可 find/call 蓝牙 HAL 与 BtTpi | 调用**打进实现**了（`bttpi-impl: UnRegisterTpiEventsCallback / Init not done`），但仍 `EX_SECURITY` ⇒ 拒绝来自 BtTpi **自己的用户态校验**，不是 binder 层 |
| v0.2 | + `allow ksu bluetooth:process { transition dyntrans }` 等 | `runcon u:r:bluetooth:s0 id` → `exec id: Permission denied` |
| v0.3 | + `process2 { nnp_transition nosuid_transition }` | 仍然 `Permission denied` ⇒ **根因：进程带 NO_NEW_PRIVS 时，SELinux 只承认"按被 exec 文件标签定义的 `type_transition`"**，`runcon` 指定目标域一律无效 |

要绕过只剩一条：`typetransition ksu shell_data_file process bluetooth`。
**但它会把所有从 `/data/local/tmp` 起的 su 进程都变成 bluetooth 域** —— 直接破坏 desk-takeover/desk-stop 与一切调试脚本的 root 语义，**代价远大于收益，不做**。

⇒ **BtTpi 放弃**。也顺带说明：B 路线（**让安卓自己上电，我们只接盘**）现在是唯一可行路径，
而且它并不需要 BtTpi：上电与固件下载全部由安卓的正常流程完成，我们只在其后抢 HAL 客户端位。

**B 路线的唯一关键未知**（必须在真 DRM 轮次里测，anland 测不准，因为 `stop` 之后 Framework/Fluoride 才真正消失）：
1. 安卓开着蓝牙时，HAL 的客户端槽位是否允许被我们 `initialize_aidl` 抢占（还是回异常）；
2. `stop` 杀掉 `com.android.bluetooth` 后，HAL 是否**不重启**并保持芯片带电
   （anland 里我用 `kill -9` 杀 App 时观察到 HAL 随之重启、但新进程立刻 `HALfd=2`，需要 DRM 场景复确认）；
3. 带电前提下 `sendHciCommand` 的码位（本机 AIDL 只有 6 个方法，候选 3/4/5/6；
   判据只用副作用：`dmesg | grep bt_power_vote` 出现新行、HAL 日志 IBS 活动、回调收到 hciEvent）。

**下一次进 DRM 的动线**（需要你点桌面快捷方式）：
- desk-takeover 里在 `stop` **之前**加 `svc bluetooth enable`（+等 IBS 活跃）；
- 接管完成后在安卓侧跑 `bthci-bridge --name`，若拿到 event 就定码位；
- desk-stop 里在 `start` 之前杀桥（`pkill -x bthci-bridge`）并 `svc bluetooth disable`，让安卓重启后自己复位。


## 23. 一次性结论：BtTpi 与 SELinux 无关（permissive 实测，09-24 22:25）

30 秒 `setenforce 0` 实验（完毕立刻 `setenforce 1`，已确认回到 `Enforcing`）：

| 测试 | 结果 |
|---|---|
| permissive 下 `runcon u:r:bluetooth:s0 id` | **成功**：`context=u:r:bluetooth:s0` ⇒ 之前 `Permission denied` 确实只是 NNP 强制阶段的拦截（内核 `process2 nnp_transition` 检查在 permissive 下被跳过） |
| permissive 下 ksu 域打 BtTpi 码 1..6 | 仍 `EX_SECURITY`（4 是 `No data available`、6 返回 `1`） |
| **permissive + bluetooth 域内**打 BtTpi | 仍 `EX_SECURITY`，并打出 `bttpi-impl: UnRegisterTpiEventsCallback: Init not done. Returning` |

⇒ **`EX_SECURITY` 是 BtTpi 自己的状态机检查**：它要求**同一客户端**先成功调过 `init(...)`，否则一律拒；与调用者 uid、SELinux 域、enforcing 与否都无关（期间 dmesg 累计 431 条 `permissive=1` 记录，只是历史日志，无残留影响）。

⇒ **A 路线彻底判死**（要走通必须穷举闭源 `init` 的参数签名：`code=4 i32 0 i32 0 → EX_SECURITY`、`i32 1 i32 1 → NO_MEMORY`，形状推不出来）。
⇒ 唯一路径是 **B 路线：让安卓自己上电，我们抢 HAL 客户端位**，它完全不需要 BtTpi。剩下的三个未知只能在真 DRM 轮次里测（§22 末尾列了动线与判据）。

**顺带记一条纪律**：`setenforce 0` 这类整机安全态翻转，做完**必须在同一个脚本里复原并打印 `getenforce` 确认**（本次就是这么做的），不要跨多次 adb 调用留着 permissive。


## 24. M1b 真因修正（DRM 轮 09-24 22:3x 实测）：**不是 udevd，是 BT 套接字枚举不到 hci0**

DRM 态现场（桥在跑、`/sys/class/bluetooth/hci0` 存在）逐项实测：

| 检查 | 结果 |
|---|---|
| udevd | **在跑**（`pgrep -x systemd-udevd`=1）；裸起的实例缺 `/run/udev/control` → 我用 `systemctl restart systemd-udevd` 补上了，`/run/udev/control` 出现 ✓ |
| udev 数据库 | **有条目**：`/run/udev/data/+bluetooth:hci0`；`udevadm info -q property -p /sys/class/bluetooth/hci0` 返回 `SUBSYSTEM=bluetooth DEVTYPE=host`（**无 DEVNAME**，正常：蓝牙 class 设备没有节点） |
| bluetoothd（重启两次，含 udevd 修好之后） | 仍 `bluetoothctl list` 空 |
| **bluetoothd `-d` 调试日志** | 决定性一行：**`src/adapter.c:read_index_list_complete() Number of controllers: 0`** |
| `hcitool dev`（容器 /usr/bin 里的二进制，Android 侧 root 执行） | **`Devices:` 空** |
| `hciconfig -a hci0`（按名字查） | **正常输出**（`Type: Primary Bus: UART`，DOWN，MAC 全零） |

⇒ **F22 的"缺 udevd"结论作废**：udev 侧一切正常，BlueZ 压根不从 udev 拿 adapter，它走 **mgmt 控制信道 `Read_Index_List`**，而**内核返回 0 个控制器**；同一时刻按**设备名**查询（`HCIGETDEVINFO`）却是成功的。也就是：**hci0 在 sysfs 里存在，但对 BT 套接字的"枚举"不可见**。

下一轮要查的两个具体方向（都已缩小到这里）：
1. **netns 归属**：`hci_register_dev` 把设备记在注册者的 netns 里，mgmt 的 `Read_Index_List` 按 `sock->sk->sk_net` 过滤，而 `HCIGETDEVINFO`（按 id/名查）在某些内核版本上**不过滤** —— 这恰好能同时解释"枚举为空、按名可查"。
   本轮我测的 netns 对照有个方法错误：`nsenter -t <pid> -m -p` **没带 `-n`**，所以那条 `readlink /proc/self/ns/net` 测的是安卓侧，不是容器的。**下次必须 `nsenter -m -p -n` 或直接 `nsenter -t <pid> -a` 测容器 netns**，再和桥所在进程比。
   若确认不同 → **修法很干净：把桥注册 hci0 的那一步放进容器的 netns**（安卓侧 root 起桥时 `nsenter -t <容器pid> -n`，只借 netns，不借 mount/pid，这样仍不需要容器 root），设备就会对容器里的 bluetoothd 可见。
2. 若 netns 相同：查 `hdev->reg_state` 与 `hci_dev_get_iter` 的过滤条件（内核里 `hci_dev_list` 遍历处是否有 `HCI_REGISTERED` 之外的门槛），并顺带核对是否因 `Read_Version` 超时把设备卡在 `HCI_INIT` 而未进链表。

**顺带记录纪律**（今天三次踩）：`pkill -f xxx` 会把自己所在的 `su -c`/`nsenter` 命令行一起杀掉（表现为整条命令 rc=143），一律用 **`pkill -x`** 或记 `$!` 按 pid 杀；`nsenter` 进容器执行时要给**绝对路径的可执行文件**或 `/usr/bin/env PATH=... /bin/sh -c`，否则 `systemctl/awk/cut` 全部 not found；`ps` 输出有前导空格，取 pid 用 `awk '{print $1}'` 不要用 `cut -d' ' -f1`。

---

## 25. ★★ 本轮最重要的两条：真实事务码表 + "event=0" 判据一直是假的（DRM 轮 09-24 22:5x–23:2x）

### 25.1 码表不再靠猜（可复用的方法）

`/vendor/lib64/android.hardware.bluetooth-V1-ndk.so` 拉到主机上反汇编，
**每个 `Bp*` 方法在 `bl AIBinder_transact` 前都有一条 `mov w1, #code`**，把 code 和符号对上就得到全表：

| IBluetoothHci | code | | IBluetoothHciCallbacks | code |
|---|---|---|---|---|
| close | 1 | | aclDataReceived | 1 |
| **initialize(cb)** | **2**（oneway） | | **hciEventReceived** | **2** |
| sendAclData(byte[]) | 3 | | **initializationComplete(Status)** | **3** |
| **sendHciCommand(byte[])** | **4** | | isoDataReceived | 4 |
| sendIsoData(byte[]) | 5 | | scoDataReceived | 5 |
| sendScoData(byte[]) | 6 | | | |

`getInterfaceVersion/Hash` 走 0xffffff/0xfffffe，不占码位。**这个接口没有 enable/disable**——
所以"缺一个 enable 方法"这个假设从一开始就不成立（电源在 BtTpi 那边，见 §20–§23）。

### 25.2 致命观测错误：`g_cbEvents` 从来没有 `++`

`--name`/S5/S6 那一整批扫描的否定判据全是 `event=0`，而那个计数器**在任何地方都没被累加**，
所以 0 是常量。**被推翻的结论**：

* §17「真上电入口 = 事务码 2，同步调用它 HAL 就开 UART」→ 错。码 2 是 **oneway**，
  同步调它会 `-2147483647`（EX_TRANSACTION_FAILED）**且不投递**；当时看到 HALfd 0→2
  是安卓自己那次 enable 留下的。**正确打法：oneway + 带回调 binder**。
* 「码 1 是 legacy initialize（no-op）」→ 错，码 1 是 `IBinder::close()`。
* §18「发出去没见芯片回 event ⇒ sendHciCommand 不在这几个码位」→ 判据无效（见上）。

**教训（写进红线旁边）**：任何"计数器判据"必须先自证会被累加——在计数器还是常量 0 时，
一整轮"否证"都是空气。以后新增观测点第一件事是**构造一个必然命中它的用例**验证探针本身。

### 25.3 修正后的首轮实测（`--cmd 01030c00` = 不经内核，直接 initialize + sendHciCommand(HCI_Reset)）

| 观测 | 结果 |
|---|---|
| `initialize`(2, oneway, 带回调) 投递 | st=0 |
| HAL 回调 | **`initializationComplete` 到了**（回调数=1）→ 客户端位坐上了 |
| HAL 传输 fd | **0 → 2**（`bt_cp_ctrl` + `ttyHS0` 被我们这次调用打开）|
| `sendHciCommand`(4) 投递 | st=0 |
| 8s 内 `hciEventReceived` | **0 次**（这次是真判据了）|
| dmesg | `btpower` 走完 `BT_CMD_ACCESS_CTRL Request→ACCESS_GRANTED→Release`，随后 `PWR_CTRL → Power Retention` |
| 之后 HAL 传输 fd | 回到 0（HAL 自己收了） |

⇒ 链路前段（binder→HAL→上电→开 UART）**已通**；断点在"芯片对 HCI_Reset 不回 event"。
dmesg 那句 `PWR_CTRL ... Power Retention` 像是 HAL 试完自己放弃了 → 下一件事就是读它到底报了什么。

### 25.4 顺带量清的两个格式细节

* **Android 15/16 的 AIDL parcel 参数起点**：`80000000 ffffffff 'SYST'(0x53595354) len UTF-16…`
  → 参数偏移 = `16 + align4(len*2+2)`。老的 2 字头算法会**少算 4 字节**，
  实测把 `Status` 读成 `6422574`（那其实是 UTF-16 的 `"re"`）。
  现在 skipToken 改成**扫描**"后面紧跟 UTF-16 ASCII 的长度字"来定位，不写死偏移。
* **回调数组带不带 H4 类型字节不能靠首字节启发式**：HCI 事件码 0x01–0x05 和类型字节取值区间重叠，
  一定读错。改成显式开关 `--cb-with-type`，由实测定死。

---

## 26. ★★★ M2/M3 实测通过：DRM 接管态下容器拿到**原生**蓝牙控制器（09-24 23:1x–23:2x）

### 26.1 端到端证据（全部一次真实运行里读到）

```
[bthci] ← initializationComplete(status=0 SUCCESS)@116 ★★★ HAL 认了我们这个客户端
[bthci] → sendHciCommand (3 字节): 09 10 00                 # HCI_Read_BD_ADDR，不带类型字节
[bthci] ← hciEventReceived(数组@116) (12 字节): 0e 0a 01 09 10 00 e0 80 a1 62 34 20
        # Command Complete / status 0 / BD_ADDR e0:80:a1:62:34:20（LSB 序）
```

进入双向搬运（`--keep 90`）后内核自己跑完了整条 init 序列（60 个命令一来一回），容器侧：

| 观测 | 结果 |
|---|---|
| `hciconfig -a`（容器 ns 内） | `hci0: Type: Primary Bus: UART`，**BD Address: 20:34:62:A1:80:E0**，`UP RUNNING INQUIRY`，ACL MTU 1024:8，Features 0xff 0xfe … |
| `hcitool dev` | **列出 hci0**（之前 M1b 的"枚举不到"就是 init 没跑完） |
| `bluetoothctl list` | **`Controller 20:34:62:A1:80:E0 Ubuntu [default]`** ← 原生 BlueZ 认领 |
| `bluetoothctl power on` | `Changing power on succeeded`，`PowerState: on` |
| `bluetoothctl scan on` | 扫到周围一片设备（含名为 `midea` 的），RSSI 正常变化 |
| `bluetoothctl show` | Manufacturer 0x001d(高通)、Version 0x0e、GATT/PnP UUID 齐 |

### 26.2 三条定死的格式事实（都是踩出来的）

1. **两个方向的 byte[] 都不含 H4 类型字节**。`011000`（裸 opcode+len）有回包，`01011000`（多带类型字节）没有。
   内核 hci_uart(H4) 那侧**必须**带类型字节 ⇒ 桥自己加/减：`g_include_type=false`、`g_cb_has_type=false` 为默认。
2. **AParcel 参数起点 = 116**（本机 initializationComplete 的 parcel 共 120 字节）：
   `80000000 ffffffff 53595354('SYST') 00000031(len=49) + UTF-16 + NUL` → `16 + align4(49*2+2) = 116`。
   `skipToken` 现在**返回**这个位置，任何一次读之前都要显式 `setDataPosition(p0)`——
   中途游标被别的代码挪走就会读错（实测把 49 字节的 descriptor 当成事件数组"读成功"了）。
3. **HAL 会吞掉它自己内部命令的回包**：日志 `OnPacketReady: Received event for command sent internally: 03 0c`
   ——`HCI_Reset` 是 HAL 初始化时自己发过的，所以它的 Command_Complete 不会转给客户端；
   客户端再发 Reset（`030c00`）实测**能**拿到回包（吞不掉时才算通路）。

### 26.3 电源这件事，最终说法

不需要 BtTpi、不需要我们碰任何电源 ioctl：`desk-takeover` 在 `stop` **之前**跑
`svc bluetooth enable`，安卓自己的 HAL 完成上电+下固件；`stop` 杀掉 framework 后
芯片仍 `BT powered ON`（dmesg `bt_power_vote: ... BT powered ON`），我们只是**再挂一个 binder 客户端**，
HAL 见新客户端 `initialize` 就重新 `OpenUart`（fd 16）并把芯片叫醒。这就是"不影响 anland"的机制保证：
不接管电源、不接管固件、不抢 `/dev/ttyHS0`（我们从头到尾没 open 过它）。

### 26.4 已接线（droid-drm-takeover，本地 commit）

* `desk-takeover.sh`：`stop` 前 `svc bluetooth enable`；桌面/WiFi 都起来后的 **5c)** 段
  `systemctl start bluetooth` + 安卓侧 `nohup bthci-bridge --keep 0 &`，`bluetoothctl list` 见
  Controller 打 `BT-NATIVE OK`，否则 `BT-NATIVE FAIL`（**不连坐桌面**）。
* `scripts/desk-stop.sh`：`start` 之前 `pkill -x bthci-bridge`（50s 看门狗里也有）；
  **4b2)** 段交还后确认无残留桥。安卓侧不需要还原——framework 一起来自己会 enable 蓝牙。

### 26.5 还欠的（M4）
0. **桌面 UI 缺件已补**：容器（Ubuntu 26.04 + Plasma 6.6.6）里**根本没装 bluedevil** →
   系统设置里没有蓝牙页（`org.bluez` 一直是好的，`bluetoothctl` 全能）。
   `apt-get install -y bluedevil`（只它自己，0 依赖churn）+ `kbuildsycoca6 --noincremental` 后页面出现。
   装法（容器 root 从安卓侧借 ns，注意 PATH 必须给全）：
   `nsenter -t <容器systemd pid> -m -p -n -- /usr/bin/env PATH=/usr/bin:/bin:/usr/sbin:/sbin apt-get install -y bluedevil`
   注意 bluedevil 的**配对 agent 是 kded 插件**（`kf6/kded/bluedevil.so`），
   新装的 kded 模块要 kded 重新扫描才加载 ⇒ 配对弹窗可能得等 plasmashell/kded 重启一次。

1. **配对信息不共享**：安卓的链路密钥在 `/data/misc/bluedroid`，BlueZ 不认，
   接管期想连耳机得**重新配对一次**（一次性成本，可接受）。
2. 交还安卓时 BlueZ 侧要 `power off`（否则 bluetoothd 还攥着 hci0 的引用）——现在靠 pty 关闭强制注销，
   需要实测一轮"交还后安卓蓝牙能正常开关、能连回耳机"。
3. `Name: Ubuntu` 是从容器主机名来的，桌面上应显式设成用户熟悉的名字（`bluetoothctl system-alias`）。
4. 未测：A2DP 音频（要 `pipewire`/`bluez5` 在 DRM 桌面侧起得来）、BLE 外设（键盘/鼠标走 BlueZ input）。

---

## 27. 更正两条（09-25 00:1x，DRM 黑屏排查带出来的）

1. **§26.3/§26.4 里的"`stop` 前 `svc bluetooth enable`"撤了，它根本不需要**：
   这台机器开机安卓自己就把蓝牙开着（用户原话"重启后自动开的我无法控制"），
   而且 23:05 那一跑实测过：HAL 冷态（`--codes` 打 `传输fd数=0`）下我们
   `initialize`(oneway,码2) 一样让它 `OpenUart` 把 `ttyHS0`/`bt_cp_ctrl` 打开（0→2）。
   ⇒ 上电/下固件由安卓开机那次自启完成，接管期桥只做"再挂一个客户端"，脚本里不再碰蓝牙开关。
2. **5c) 起桥默认关闭**（`BT_BRIDGE=1` 才开）。原因不是蓝牙有害，而是同窗口出现了
   DRM 桌面黑屏回归（真因见工作总结 5.28：plasma 那行 `env` 参数顺序错），
   在显示链路重新验证干净之前，不让第二条改动同时上默认路径——一次只动一个变量。
   `desk-stop` 里 `pkill -x bthci-bridge`（主流程 + 50s 看门狗）**保留**，那是无害的保险。

顺带一句方法论：本轮我先怀疑自己的 `svc bluetooth enable` 造成"整机复位"，
用 pstore 复核后发现既没有 panic/oops、`mtdoops reason=7` 前后都是正常触摸日志，
"console 静默 86s" 也只是 `stop` 之后 servicemanager 的 spam 自然停了 ——
**那两次是用户长按电源强启**。归因未落实前先别改自己的因果结论，先查 pstore/dmesg 的最后一行。
